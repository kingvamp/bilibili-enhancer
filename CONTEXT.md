# CONTEXT.md

## 当前进度
- 修复了工具栏“一键收藏”按钮在播放列表（如稍后观看）切换视频时不刷新状态的问题。

## 上次停留在
- 已完成代码修改并编写了修复总结。

## 关键决定
- 在 `ToolbarManager` 中增加对 `location.href` 的定时轮询监听（1秒一次），用于弥补 `MutationObserver` 在某些 SPA 导航场景下无法触发刷新的缺陷。
- 在检测到 URL 变化后，设置 500ms 的延迟再执行刷新，以确保原生 DOM 渲染基本完成。
