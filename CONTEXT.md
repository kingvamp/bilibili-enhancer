# CONTEXT.md

## 当前进度
- 已完成全工程网站元素选择器的抽象与重构。
- 修复了时长筛选功能在重构后因选择器范围过广导致的误隐藏问题。
- 已统一管理 `selectors.ts` 和 `dom.ts`。
- 修复了投稿页影片卡片标题高亮失效的问题。
- 实现了在设置面板中显示已下载视频数量的功能。
- 实现了全站通用的悬浮筛选菜单功能，支持在任意页面快速调节时长和充电过滤。
- 优化了充电专属视频的遮罩样式：仅覆盖封面、黑色透明背景且不拦截点击。
- 新增了充电专属角标功能：即使在关闭拦截模式下，也会为搜索页等区域的充电视频添加醒目的粉色角标。

## 上次停点
- 完成了充电专属角标功能的开发与多模式集成。

## 近期关键决定
- **悬浮筛选菜单**：为特定高频过滤页面（投稿、搜索、稍后再看）引入毛玻璃风格的悬浮菜单，减少用户进入设置页面的频率。
- **选择器分层**：将 `VIDEO_CARD` 选择器拆分为 `ENTITY`（扫描专用）和 `ANCESTORS`（向上查找专用），解决了 `durationFilter` 误伤父容器的问题。
- 封装了 `findTitleInCard` 和 `extractBvid` 等通用工具，提高了新功能的开发效率和代码复用性。
