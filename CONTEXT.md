# CONTEXT.md

## 当前进度
- 已完成全工程网站元素选择器的抽象与重构。
- 修复了时长筛选功能在重构后因选择器范围过广导致的误隐藏问题。
- 已统一管理 `selectors.ts` 和 `dom.ts`。
- 修复了投稿页影片卡片标题高亮失效的问题。
- 实现了在设置面板中显示已下载视频数量的功能。
- 实现了全站通用的悬浮筛选菜单功能，支持在任意页面快速调节时长和充电过滤。
- 优化了充电专属视频的遮罩样式：仅覆盖封面、黑色透明背景且不拦截点击。
- 新增了充电专属角标功能：即使在关闭拦截模式下，也会为搜索页等区域的充电视频添加醒目的粉色角标。
- **优化与重构**：完成了悬浮筛选菜单样式的解耦，将内联样式 `MENU_STYLE` 抽离为独立的外部 CSS 文件 `floatingFilterMenu.css`。
- **新增功能**：在悬浮过滤菜单中新增了分辨率过滤功能，支持过滤屏蔽 8K/4K/2K/1080P/720P/SD 分辨率视频；新增了一键清空重置所有筛选设置功能。
- **体验与视觉优化**：重构了悬浮筛选菜单的布局与功能，实现了拖拽位置百分比记忆、5区域智能防溢出与对齐展开（左边缘向右展开，靠左部分向左展开，中间居中，靠右部分向右展开，右边缘向左展开）、药丸式标签复选框、无感双击确认重置、未启用项状态禁用变灰以及激活状态的呼吸灯闪烁指示角标。

## 当前正在做什么
- 已完成悬浮筛选菜单智能对齐展开规则的调优、编译和校验。

## 上次停在哪个位置
- 完成了 5 区域智能防溢出与对齐展开的参数调整与代码编译通过。

## 本次修改的提交说明
- refactor: 优化悬浮筛选菜单在不同屏幕位置的对齐规则，实现5区域自适应展开逻辑

## 近期的关键决定
- **样式抽离决策**：选择将悬浮菜单的样式抽离到独立 CSS 样式表中，使单个 TS 文件维持在 500 行以内，大大提高了可读性和 IDE 的 CSS 语法高亮/智能感知开发体验。
- **引入 FilterEngine**：为了统一管理 DOM 扫描和视觉效果，避免多个 Observer 冲突。
- **引入 DataCenter**：响应用户建议，将缓存逻辑从 API 调用中解耦，实现真正的数据中心。
- **选择器分层**：将 `VIDEO_CARD` 选择器拆分为 `ENTITY`（扫描专用）和 `ANCESTORS`（向上查找专用），解决了 `durationFilter` 误伤父容器的问题。
- 封装了 `findTitleInCard` 和 `extractBvid` 等通用工具，提高了新功能的开发效率和代码复用性。
